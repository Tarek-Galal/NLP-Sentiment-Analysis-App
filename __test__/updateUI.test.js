import { updateUI } from "../src/client/js/updateUI";

describe("Testing the updateUI function", () => {
  test("The updateUI result should be defined", () => {
    expect(updateUI).toBeDefined();
  });
});
